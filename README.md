- Files aren't actually interpretted, so no Type aliases
- All fields must have a type (use Any)
- No inheritance

default modeldoc.yaml:
```
inputDirectory: models/
docDirectory: docs/
docModelDirectory: models/
navFolder: Models
mkdocsFile: mkdocs
```